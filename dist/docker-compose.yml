version: '3'
services:
  develop:
    image: etable/php:dev-n-build
    volumes:
       - ${CODE_BASE_PATH}:/home/developer/code

  nginx:
    image: nginx:alpine
    ports:
      - 7080:7080
    links:
      - develop
    volumes:
      - ${CODE_BASE_PATH}:/home/developer/code
